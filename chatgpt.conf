[General]
# Use ad-blocking DNS
dns-server = 94.140.14.14, 94.140.15.15 # AdGuard DNS servers

# Fallback DNS servers (optional, in case AdGuard is unavailable)
fallback-dns-server = 9.9.9.9, 149.112.112.112 # Quad9 DNS servers

# Proxy settings
bypass-system-dns = true
dns-fallback = true

[Proxy]
# Configure proxy for ChatGPT-related domains
# proxy = "socks5://127.0.0.1:1080"

[Rules]
# Route traffic for ChatGPT-related domains through the proxy
DOMAIN-SUFFIX,openai.com,proxy
DOMAIN-SUFFIX,chat.openai.com,proxy
DOMAIN-SUFFIX,api.openai.com,proxy

# (Optional) Add additional related domains if required
DOMAIN-SUFFIX,openai.net,proxy

# Default rule: Use direct for all other traffic
FINAL,DIRECT